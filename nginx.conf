server {
    listen       80;
    server_name  localhost;

    #access_log  /var/log/nginx/host.access.log  main;

    location ^~ /backend {
        proxy_pass   http://backend:9999$request_uri;
    }

    location / {
        proxy_pass   http://frontend:80;
    }
}
